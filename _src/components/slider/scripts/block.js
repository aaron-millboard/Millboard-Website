// import Slider from './Slider.js';

// window.addEventListener('DOMContentLoaded', () => {
//     const items = document.querySelectorAll('.slider');

//     [...items].forEach((item) => {
//         // Read configuration from data attributes
//         const config = {
//             // itemsInView: parseInt(item.dataset.itemsInView, 10) || 1,
//             enableKeyboard: item.dataset.enableKeyboard !== 'false',
//             enableTouch: item.dataset.enableTouch !== 'false',
//             respectReducedMotion: item.dataset.respectReducedMotion !== 'false',
//             transitionDuration: item.dataset.transitionDuration || '300ms',
//         };

//         new Slider(item, config);
//     });
// });
